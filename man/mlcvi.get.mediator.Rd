% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_mediator.R
\name{mlcvi.get.mediator}
\alias{mlcvi.get.mediator}
\title{Run multi-mediator Sobel screen (MLCVI-style) and return significant mediators}
\usage{
mlcvi.get.mediator(
  df = NULL,
  path = NULL,
  sheet = NULL,
  iv,
  dv,
  mediator_names = NULL,
  mediator_regex = "^mlcvi([1-9]|[1-5][0-9]|60)$",
  expect_n = 60,
  require_exact_count = TRUE,
  allow_abc_fallback = TRUE,
  alpha = 0.05,
  adjust = "BH",
  ignore_case = TRUE,
  na_action = c("complete.cases", "none"),
  coerce_iv01 = TRUE
)
}
\arguments{
\item{df}{A data.frame containing IV, DV, and mediator columns. Optional if `path` is given.}

\item{path}{Path to an Excel file (optional). If provided, data are read via readxl.}

\item{sheet}{Sheet name or index for Excel (optional).}

\item{iv}{Character scalar: column name of the binary independent variable (0/1).}

\item{dv}{Character scalar: column name of the dependent variable.}

\item{mediator_names}{Optional character vector of mediator column names to use directly.}

\item{mediator_regex}{Optional regex to auto-detect mediators from names(df).
Defaults to `^mlcvi([1-9]|[1-5][0-9]|60)$` (case-insensitive).}

\item{expect_n}{Integer: expected number of mediators (default 60). Used for checks.}

\item{require_exact_count}{Logical; if TRUE (default), error if `expect_n` not matched.}

\item{allow_abc_fallback}{Logical; if TRUE (default), if no MLCVI mediators are found
the function will also try `abc1..abc60` and **use those names as-is**.}

\item{alpha}{Numeric; significance level after p-adjustment (default 0.05).}

\item{adjust}{Character; p.adjust method (default "BH").}

\item{ignore_case}{Logical; case-insensitive matching for regex (default TRUE).}

\item{na_action}{One of `"complete.cases"` (default) or `"none"`. If `"complete.cases"`,
per-mediator fits use rows complete on {IV, DV, mediator}.}

\item{coerce_iv01}{Logical; if TRUE (default), coerce non-numeric two-level IV to 0/1.}
}
\value{
A list with:
\itemize{
  \item \code{results}: data.frame sorted by \code{p_adj}, \code{sobel_p} with columns:
    mediator, n, a, a_se, b, b_se, indirect, sobel_z, sobel_p, p_adj,
    c_total, c_prime, prop_mediated
  \item \code{significant_items}: character vector of mediators with \code{p_adj <= alpha}
  \item \code{meta}: list with iv, dv, alpha, adjust, n_mediators, matched_names
}
}
\description{
Generic engine to screen many mediators between a binary culture IV (0/1)
and a continuous DV. You can pass a data.frame (`df`) or a path to an Excel file
(`path`, requires readxl). By default it looks for mediators named
`MLCVI1..MLCVI60` (case-insensitive). If none are found and
`allow_abc_fallback = TRUE`, it will also accept `abc1..abc60` **without renaming**.
}
\examples{
\dontrun{
# Using a data.frame already in memory:
out <- mlcvi.get.mediator(df = mydata, iv = "US0IN1", dv = "envavg")
out$significant_items
head(out$results, 10)

# Using an Excel file (requires readxl installed):
out <- mlcvi.get.mediator(
  path = "Study 4a_Data.xlsx", iv = "US0IN1", dv = "envavg"
)
}
}
